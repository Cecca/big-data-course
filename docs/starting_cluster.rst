Starting a cluster from scratch
===============================

Hopefully you will not have to do this.
However, this guide is here in case some disaster happens, and everything needs to be started again from scratch.

The following video shows how to start a cluster from scratch.

.. raw:: html

   <video controls width="100%">
     <source src="_static/test.ogv" type="video/ogg">
   </video>

After all the machines have started (go and have a break, it takes quite a while) we can :ref:`setup our ssh access <ssh_setup>` to each one of them.
